import React, { FC } from 'react';
import SearchForm from '../SearchForm/SearchForm';

const Header: FC = () => {
    return (
        <header>
            <SearchForm />
        </header>
    );
};

export default Header;